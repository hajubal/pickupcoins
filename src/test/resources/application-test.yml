spring:
  jpa:
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:~/coins;AUTO_SERVER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
slack:
  webhook:
    url: https://hooks.slack.com/services/~

cookie:
  naver: ha:~~
