spring:
  profiles:
    active: test
  security:
    user:
      name: test
      password: test

logging:
  level:
    me.synology.hajubal.coins: debug
    org:
      springframework: info
      hibernate:
        #print logger
        SQL: debug
        #parameter mapping (p6spy-spring-boot-starter)
        type: trace
      apache:
        coyote:
          http11: debug