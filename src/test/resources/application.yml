spring:
  profiles:
    active: test

logging:
  level:
    me.synology.hajubal.coins: debug
    org:
      springframework: info
      hibernate:
        #print logger
        SQL: debug
        #parameter mapping (p6spy-spring-boot-starter)
        type: trace
      apache:
        coyote:
          http11: debug