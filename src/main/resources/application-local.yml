spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:h2:~/coins;AUTO_SERVER=TRUE;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
  sql:
    init:
      mode: never

logging:
  file:
    path: /tmp/log/pickupcoins
