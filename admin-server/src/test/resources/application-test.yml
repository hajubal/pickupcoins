spring:
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
  sql:
    init:
      mode: always
  thymeleaf:
    cache: false

logging:
  file:
    path: /tmp/log/pickupcoins
